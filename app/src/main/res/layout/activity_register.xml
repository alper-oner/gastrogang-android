<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".LoginActivity">

    <include layout="@layout/actionbar_app"
        android:id="@+id/actionbarRegister" />

    <EditText
        android:inputType="text"
        android:id="@+id/txtUsernameRegister"
        android:hint="@string/username"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="150dp"
        android:layout_marginLeft="18dp"
        android:layout_marginRight="18dp"
        android:padding="8dp"
        android:background="#fff" />

    <EditText
        android:inputType="textPassword"
        android:id="@+id/txtPasswordRegister"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="18dp"
        android:layout_marginRight="18dp"
        android:padding="8dp"
        android:background="#fff"
        android:hint="@string/password"
        android:layout_marginTop="12dp"
        android:layout_below="@+id/txtUsernameRegister" />

    <EditText
        android:inputType="text"
        android:id="@+id/txtLifetime"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="18dp"
        android:layout_marginRight="18dp"
        android:padding="8dp"
        android:background="#fff"
        android:hint="Lifetime of recipes in days"
        android:layout_marginTop="12dp"
        android:layout_below="@+id/txtPasswordRegister" />

    <Button
        android:id="@+id/buttonRegister"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/register"
        android:textColor="#00CC99"
        android:layout_below="@+id/txtLifetime"
        android:layout_marginTop="17dp"
        android:layout_alignStart="@+id/txtLifetime"
        android:layout_alignEnd="@+id/txtLifetime" />

</RelativeLayout>